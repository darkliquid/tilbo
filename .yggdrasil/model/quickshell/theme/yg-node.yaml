name: Theme Service
type: module
description: "QML singleton providing centralized color palette with Nord-inspired defaults and external config override"

mapping:
  paths:
    - internal/quickshell/services/Theme.qml
